# Brute

INSTALL TOOLS

$ pkg update && upgrade

$ pkg install python -y

$ pkg install git -y

$ pip install rich requests bs4

$ rm -rf Brute

$ git clone https://github.com/XyraXD44/Brute

$ cd Brute

$ python main.py
